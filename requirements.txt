# Python deps for dvc_cosmos_nano.
#
# Not installable with pip, but still required:
#   ffmpeg + ffprobe on PATH   frame sampling and chunk cutting
#
# `python infer.py --selfcheck` runs with none of this installed.

torch>=2.4
torchvision                # Cosmos3-Nano's processor loads Qwen2VLImageProcessor, which requires this
pillow                     # required by the same image processor
torchcodec                 # video decoding for chat-template video inputs; without it transformers
                           # falls back to torchvision's deprecated (and on some versions broken,
                           # e.g. torchvision>=0.23 dropped io.read_video) video reader
transformers>=5.11        # Cosmos3OmniForConditionalGeneration
accelerate>=0.26          # device_map="auto"
av>=13                    # used by some transformers video paths besides the chat-template one
huggingface_hub>=0.34     # weight download, and the `hf` CLI

# server/ (web UI) only
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9   # multipart form parsing (video upload)
